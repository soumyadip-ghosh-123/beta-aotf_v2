---
title: API Overview
description: Conventions, authentication, error handling, and response formats used across all API endpoints.
---

## Base URL

All API endpoints are versioned under:

```
/api/v1/*
```

---

## Authentication

Most endpoints require a valid Clerk session. The middleware (`proxy.ts`) handles authentication before requests reach route handlers.

| Header | Value | Required |
|--------|-------|----------|
| `Authorization` | `Bearer <clerk-session-token>` | For protected endpoints |
| `Content-Type` | `application/json` | For POST/PATCH/PUT requests |

> The Clerk session token is automatically attached by the Clerk SDK on the client side.

---

## Request Conventions

### CSRF Protection

All mutation requests (POST, PATCH, DELETE) are validated against the `Origin` or `Referer` header. The request must originate from the same host.

### Rate Limiting

Sensitive endpoints enforce rate limits. When exceeded, the response includes:

```json
{
  "error": "Too many requests. Please try again later."
}
```

With headers:

```
HTTP/1.1 429 Too Many Requests
Retry-After: 45
X-RateLimit-Remaining: 0
X-RateLimit-Reset: 1717500000000
```

---

## Response Format

### Success Response

```json
{
  "success": true,
  "data": { ... }
}
```

Or for list endpoints:

```json
{
  "success": true,
  "data": [ ... ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 150
  }
}
```

### Error Response

```json
{
  "error": "Human-readable error message"
}
```

For validation errors:

```json
{
  "error": "Validation failed",
  "fieldErrors": {
    "email": ["Invalid email format"],
    "phone": ["Phone number is required"]
  }
}
```

---

## HTTP Status Codes

| Code | Meaning | When Used |
|------|---------|-----------|
| `200` | OK | Successful GET, PATCH |
| `201` | Created | Successful POST (resource created) |
| `400` | Bad Request | Validation failure, malformed JSON |
| `401` | Unauthorised | Missing or invalid auth token |
| `403` | Forbidden | Insufficient permissions, CSRF failure |
| `404` | Not Found | Resource doesn't exist |
| `409` | Conflict | Duplicate entry |
| `415` | Unsupported Media Type | Missing `application/json` content type |
| `429` | Too Many Requests | Rate limit exceeded |
| `500` | Internal Server Error | Unexpected server error |

---

## Error Classes

The API uses a hierarchy of custom error classes (see `lib/errors.ts`):

| Class | Status | Use Case |
|-------|--------|----------|
| `ValidationError` | 400 | Input validation failure with field-level detail |
| `NotFoundError` | 404 | Resource lookup returned nothing |
| `ConflictError` | 409 | Duplicate entry or conflicting state |
| `InternalError` | 500 | Unexpected failures (never leaks internals) |

---

## Endpoint Categories

| Category | Base Path | Auth Required |
|----------|-----------|---------------|
| [Admin Management](/docs/api-reference/admin-endpoints) | `/api/v1/admin/*` | ✅ Admin |
| [Posts (Tuitions)](/docs/api-reference/posts-endpoints) | `/api/v1/posts/*` | Varies |
| [Jobs](/docs/api-reference/jobs-endpoints) | `/api/v1/jobs/*` | Varies |
| [Applications](/docs/api-reference/applications-endpoints) | `/api/v1/posts/*/applications` | ✅ User |
| [Enquiries](/docs/api-reference/enquiry-endpoints) | `/api/v1/enquiry/*` | Varies |
| [Payments](/docs/api-reference/payments-endpoints) | `/api/v1/payments/*` | ✅ User |
